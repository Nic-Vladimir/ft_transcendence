name: CI

on:
  push:
  pull_request:

jobs:
  gateway-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create env file
        run: |
          cat > .env <<EOF
          POSTGRES_USER=user
          POSTGRES_PASSWORD=pass
          POSTGRES_DB=transcendence
          DATABASE_URL=postgres://user:pass@database:5432/transcendence
          ADMIN_EMAIL=admin@admin.com
          ADMIN_PASSWORD=adminpass
          EOF

      - name: Build and start stack
        run: docker compose up --build -d

      - name: Wait for services
        run: sleep 15

      - name: Check HTTP redirect
        run: curl -I --fail http://localhost:8080

      - name: Check HTTPS gateway health
        run: curl -k --fail https://localhost:8443/health

      - name: Optional app check (non-blocking)
        run: curl -k -I https://localhost:8443 || true

      - name: Show logs on failure
        if: failure()
        run: |
          docker compose ps
          docker compose logs caddy
          docker compose logs backend
          docker compose logs frontend
          docker compose logs database
